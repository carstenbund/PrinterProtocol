# Printer Protocol (.NET)

This directory contains a simple console runner that demonstrates
interpreting the shared JSON command stream against the .NET drivers.

## Running

```bash
dotnet run
```

The sample assumes that `python/examples/label.json` exists. Generate it
from the Python emitter example first.
